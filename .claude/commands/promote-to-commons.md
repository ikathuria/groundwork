---
description: Scan all hypothesis wikis, find entity pages (methods, concepts, datasets, open-problems) that appear in 2+ topics, and promote them to `commons/`. Updates all cross-wiki wikilinks. Run periodically to grow the shared knowledge layer.
argument-hint: (no arguments)
allowed-tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Bash
---

# Promote to Commons

You are running the **promote-to-commons** skill for GROUNDWORK. **Read `context.md` first** if you haven't this session.

No arguments are required — this command scans the entire `hypotheses/` tree automatically.

Delegate immediately to the skill spec at `.claude/skills/promote-to-commons.md`.
