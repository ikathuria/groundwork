ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=AIza...

# Fallback: if ephemeral-token minting fails (e.g. account doesn't have Live token
# access yet), the browser connects directly with this key. Visible in JS bundle —
# only use for local demos, never deploy with it set.
NEXT_PUBLIC_GEMINI_API_KEY=AIza...
